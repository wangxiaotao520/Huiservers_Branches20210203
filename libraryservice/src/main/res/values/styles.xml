<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="my_dialog" parent="android:Theme.Dialog">
    <item name="android:windowFrame">@android:color/transparent</item> <!-- 去除边框 -->
    <item name="android:windowIsFloating">true</item> <!-- 是否浮现在activity之上 -->
    <item name="android:windowIsTranslucent">true</item> <!-- 半透明 -->
    <item name="android:windowNoTitle">true</item> <!-- 无标题 -->
    <item name="android:windowBackground">@android:color/transparent</item> <!-- 自己想要的背景 -->
    <item name="android:backgroundDimEnabled">false</item> <!-- 背景遮罩层 -->
    <!--<item name="android:windowAnimationStyle">@style/AnimationActivity</item>-->
</style>
    <style name="my_dialog_DimEnabled" parent="android:Theme.Dialog">
    <item name="android:windowFrame">@android:color/transparent</item> <!-- 去除边框 -->
    <item name="android:windowIsFloating">true</item> <!-- 是否浮现在activity之上 -->
    <item name="android:windowIsTranslucent">true</item> <!-- 半透明 -->
    <item name="android:windowNoTitle">true</item> <!-- 无标题 -->
    <item name="android:windowBackground">@android:color/transparent</item> <!-- 自己想要的背景 -->
    <item name="android:backgroundDimEnabled">true</item> <!-- 背景遮罩层 -->
    <!--<item name="android:windowAnimationStyle">@style/AnimationActivity</item>-->
</style>
    <style name="AppCompatTheme.Base" parent="Theme.AppCompat">
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>

        <!-- Actionbar color -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <!--Status bar color-->
       <item name="colorPrimaryDark">@color/status_bar_color</item>
        <!--Window color-->
        <item name="android:windowBackground">@color/windowbackground</item>
    </style>
    <!-- 弹出菜单 -->
    <style name="popUpwindow_anim">
        <item name="android:windowEnterAnimation">@anim/push_up_in</item>
    </style>
</resources>
